# Core computer vision
# NOTE: opencv-python-headless is used because this pipeline never calls
# cv2.imshow/GUI functions (frames are annotated and saved, not displayed
# in a native window). This avoids requiring system GUI libraries (libGL
# etc.) on servers/containers. Swap to `opencv-python` locally if you
# want to use OpenCV's own preview windows during development.
opencv-python-headless==4.10.0.84
ultralytics==8.3.28
torch==2.4.1
torchvision==0.19.1
numpy==1.26.4

# Tracking
# NOTE: ByteTrack is integrated via Ultralytics' built-in tracker (bytetrack.yaml)
# starting Phase 4, so no separate ByteTrack package is required at this stage.

# Backend
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1

# Database (Phase 12)
sqlalchemy==2.0.36

# Config / utilities
python-dotenv==1.0.1
PyYAML==6.0.2

# Testing
pytest==8.3.3
httpx==0.27.2
